{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "break",
  "description": "Capability for the break window",
  "windows": ["break"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:event:default"
  ]
}
